# 複製成 .env.development（本機）後填入實際值。
# .env.production 已 commit 進版本庫，只含公開的 mock 開關；正式 API domain 決定後在該檔設定。
# 任何私密金鑰都不可用 VITE_ 前綴，那會被打包進瀏覽器 JS。

# 未設定時：dev 預設 mock，正式 build 預設走 REST。
VITE_USE_MOCK_API=true
VITE_API_BASE_URL=
